/**
 * Next.js Configuration
 * App Router configuration, environment variables, API rewrites
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure TypeScript paths are resolved correctly
  transpilePackages: [],
};

module.exports = nextConfig;

