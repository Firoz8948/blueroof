/**
 * Lead Database Model
 * PostgreSQL table schema for leads (Mini CRM Core)
 * Fields: id, name, email, phone, source (google/instagram/facebook), status, 
 * assigned_to (user_id), property_id, created_at, updated_at
 */

export {};

